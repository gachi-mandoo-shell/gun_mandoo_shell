{
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "/usr/bin/make",
            "args": [
                "-C",
                "${workspaceFolder}/run_test",
            ],
            "options": {
                "cwd": "${workspaceFolder}/run_test",
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "디버거에서 생성된 작업입니다."
        }
    ],
    "version": "2.0.0"
}